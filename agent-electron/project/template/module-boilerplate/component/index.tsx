import type { RootState } from "type/rootState";
import { joinLessPrefix } from "utils/framework";
import "./index.less";

interface {1} {}

function {2}(props: {1}) {
    return <div className={joinLessPrefix("{3}-page")}>Hello {2}</div>;
}

export default {2};
