[package]
name = "gal"
version = "0.1.0"
description = "GAL Interactive Movie Game Editor & Player"
authors = ["roaikim"]
edition = "2021"

[lib]
name = "gal_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
