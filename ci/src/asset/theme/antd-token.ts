import { AliasToken } from "antd/es/theme/interface";

const antdCSSToken: Partial<AliasToken> = {
    fontSize: 12,
    colorText: "rgba(0, 0, 0, 0.70)",
    borderRadius: 2,
};

export { antdCSSToken };
